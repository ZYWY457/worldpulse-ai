先启动后端 API：

conda activate 检测工具
cd d:\CODE\ProjectFile\python\worldpulse-ai
uvicorn api_server:app --host 127.0.0.1 --port 8000 --reload

再开一个新的 PowerShell 窗口启动前端：

cd d:\CODE\ProjectFile\python\worldpulse-ai\frontend
npm run dev